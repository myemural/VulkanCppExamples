set(CURRENT_TARGET_NAME CreatingWindow)
set(CURRENT_EXAMPLE_NAME "Creating a Window")
set(CURRENT_LIB_NAMES WindowWrapper)

include(BuildTarget)

build_target(${CURRENT_TARGET_NAME} "${CURRENT_LIB_NAMES}" "${CURRENT_EXAMPLE_NAME}")